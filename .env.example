# CNiS-MCP Server Configuration
# Copy this file to .env and configure your settings

# Server Mode
HTTP_MODE=false                    # Set to 'true' for HTTP server, 'false' for STDIO MCP
PORT=3000                         # HTTP server port (when HTTP_MODE=true)

# News Collection Settings
CACHE_TTL_MINUTES=15              # How long to cache news data (minutes)
NEWS_FETCH_INTERVAL=900000        # How often to fetch news (milliseconds, 15min = 900000)
MAX_NEWS_AGE_HOURS=24            # Maximum age of news to include (hours)

# Analysis Thresholds
CREDIBILITY_THRESHOLD=40          # Minimum credibility score to consider reliable
IMPACT_THRESHOLD=50               # Minimum impact score to flag as important

# Optional API Keys (for future enhancements - currently not required)
# All current functionality works with 100% free RSS feeds
NEWS_API_KEY=your_news_api_key_here                     # NewsAPI.org key (100 requests/day free)
CRYPTOCOMPARE_KEY=your_cryptocompare_api_key_here                # CryptoCompare key (100k/month free)
MESSARI_KEY=your_messari_api_key_here                      # Messari key (20 req/min free)

# Database Connections (for future enhancements)
MONGODB_URL=your_mongodb_connection_string_here                      # MongoDB connection string
REDIS_URL=your_redis_connection_string_here                        # Redis connection string
POSTGRESQL_URL=your_postgresql_connection_string_here                   # PostgreSQL connection string

# Logging Level
LOG_LEVEL=INFO                    # DEBUG, INFO, WARN, ERROR

# Timezone
TZ=UTC                           # Timezone for timestamps